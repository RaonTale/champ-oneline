# 데미지 계산기 엔진 고도화 업데이트 로드맵 (Update Roadmap)

본 로드맵은 특성 변환 로직, 능력치 결합형 랭크업 파서, 도구 배수 분리 등 핵심 계산식 변경 사항을 안정적으로 반영하기 위한 단계별 개발 계획입니다.

---

## 🚀 [Phase 1] 랭크업 파서(Parser) 개정 및 입력법 도입 (최우선 과제)
공격/특공 중심의 기존 랭크 체계를 전면 개편하여, 타 능력치 랭크업을 기반으로 하는 기술(바디프레스, 어시스트파워 등)의 연산 기반을 다집니다.

* **하이브리드 능력치 문자열 파서 구현**
  * `+[랭크수치][능력치기호][기본값]` (예: `+2b32`, `+2a32`) 포맷을 인식하는 정규식(Regex) 엔진 개발
  * 문자열에서 랭크업 수치와 순수 능력치 포인트를 정확히 분리하는 인터프리터 작성
* **특수 기술 데미지 매핑 로직 추가**
  * **바디프레스**: 공격력 자리에 `b(방어)`의 랭크업 플래그가 붙은 능력치를 대입하는 예외 처리 루틴 구현
  * **어시스트파워**: 파서가 감지한 모든 능력치(a, b, c, d, s)의 랭크업 총합을 실시간 계산하여 기술 기본 위력에 `(랭크 총합 × 20)`을 더하는 동적 위력 변수 연동

---

## 🧪 [Phase 2] 도구 배수 분리 및 변환자재 타입 임의 변경 (기능 고도화)
데미지 계산 공식의 정확도를 본가 게임과 100% 일치시키고, 타입 가변 특성을 지원합니다.

* **도구 연산 로직 독립 (데미지 공식 전면 수정)**
  * 기존 기술 자체 '기본 위력'에 도구 배수를 곱하던 하드코딩 방식 제거
  * 최종 데미지 산출식 내부에서 `[도구 보정 배수]`를 별도 레이어로 분리 ex. 위력100 * 힘의 머리띠 1.1
  생명의 구슬은 별도 레이어로 적용되어 있는데 다른 위력업 도구는 아닌것 같아
* **변환자재 / 리베로 타입 가변 시스템**
  * 계산 실행 직전 포켓몬 데이터 객체의 `Type` 필드를 사용자가 임의로 런타임 중에 오버라이딩(Overriding)할 수 있는 파서 ~타입 추가

---

## 📦 [Phase 3] 생명의 구슬 복구
확실히 예전에 했던 건 잘못된거 같아 원래대로 1.3배로 돌리자

## 📦 [Phase 4] 메가진화 도구 리스트 하위
리자몽Y 라고 입력했을때 메가도구가 리스트 위에 있어서 메가리자몽Y가 하위순위로 편의성 문제야
메가진화도구는 잘 쓰이지 않으니 리스트에서 하위에 두는게 맞아

## 📦 [Phase 5] 종합 입력 가이드 업데이트 및 통합 테스트
새로운 입력법을 사용자에게 전파하고 시스템의 안정성을 검증합니다.

* **입력법 도움말 문서(README) 전체 교정**
  * 앞서 변경된 `+2b32` 표기법, 도구 배수 분리 개념, 변환자재 활용법을 담은 도움말 등 전반적인 도움말 수정